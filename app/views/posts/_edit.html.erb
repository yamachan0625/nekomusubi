<div class='modal'>

  <%= fa_icon "times-circle" %>

  <div class='post-content'>

    <%= form_for post do |f| %>

      <label>
        <div id='image-field'>
        <%= image_tag post.image.to_s, id: :edit_img_prev %>
          <div class="edit-image-field">
            <%= fa_icon "image" %>
            <%= f.file_field :image%>
          </div>
        </div>
      </label>
      <%= f.check_box :remove_image %> 画像を削除

      <div class="post-field">
          <label>タイトル<span>必須</span></label>
          <%= f.text_field :title, placeholder: 'タイトルをつけてください', class: :post_input %>
          <label><span class='title-label'>入力してください</span></label>
        </div>

        <div class="post-field">
          <label class='detail'>詳細<span>必須</span></label>
          <%= f.text_area :content, placeholder: '現在の状況を書いてください', class: :post_input %>
          <label><span class='detail-label'>入力してください</span></label>
        </div>

        <div class="post-field">
          <label class='detail'><span>変更できません</span></label>
          <div id= "address-field", style="display: block;">
            <%=@post.address%>
          </div>
        </div>
      
        <div class="post-actions">
          <div class="back-button">
            戻る
          </div>
          <%= f.submit "更新する", id: 'new-post-btn'%>
        </div>
    <% end %>
        
  </div>

</div>





